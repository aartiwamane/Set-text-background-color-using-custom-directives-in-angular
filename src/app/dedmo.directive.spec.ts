import { DedmoDirective } from './dedmo.directive';

describe('DedmoDirective', () => {
  it('should create an instance', () => {
    const directive = new DedmoDirective();
    expect(directive).toBeTruthy();
  });
});
